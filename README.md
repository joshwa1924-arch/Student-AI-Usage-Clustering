# Student-AI-Usage-Clustering
This project analyzes student AI assistant usage patterns using Hierarchical and K-Means Clustering techniques. Student sessions were grouped based on prompts, session length, task type, AI assistance level, and satisfaction rating. PCA, silhouette score, elbow method, and dendrogram analysis were used to identify meaningful user behaviour patterns
